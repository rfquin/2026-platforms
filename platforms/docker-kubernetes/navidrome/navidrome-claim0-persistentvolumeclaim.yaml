apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: navidrome-claim0
  name: navidrome-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
